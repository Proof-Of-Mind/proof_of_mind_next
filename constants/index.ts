const CLAIM_CREATES = `This signature is not used to evoke transactions and is only used by the VRF to generate random numbers to be provided for use in box generation:\n$`
const SET_REFERRAL = `This signature is used to set the referral address:\n$`
const BURN_CREATES = `This signature is used to burn the gold: $ and #`;

export {
    CLAIM_CREATES,
    SET_REFERRAL,
    BURN_CREATES
}